# Gere um valor aleatório forte, por exemplo com: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Nunca use um valor previsível como o deste exemplo em produção.
JWT_SECRET=troque-por-um-valor-aleatorio-gerado-por-voce
DATABASE_URL=
# Domínio final do sistema publicado (restringe o CORS). Ex: https://sisgp.onrender.com
ALLOWED_ORIGIN=
